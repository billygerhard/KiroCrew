{
  "name": "spec-engine",
  "version": "0.1.0",
  "displayName": "Spec Engine",
  "description": "Turn a request into a reviewed spec and then execute it. Any agent that can call MCP tools drives the whole workflow: the engine vends the authoring, orchestration and review instructions, validates the native documents it produces, and refuses to advance a phase whose document does not validate or lacks a live approval. Nothing about the format lives in a prompt an agent can forget or paraphrase.",
  "author": "kirocrew",
  "tags": [
    "specs",
    "planning",
    "workflow",
    "agents"
  ],
  "defaultEnabled": false,
  "skills": [
    "skills/spec-engine-discovery"
  ],
  "mcpServers": {
    "spec-engine": {
      "command": "python3",
      "args": [
        "-m",
        "kiro_crew.apps.builtins.spec_engine.engine_mcp.server"
      ]
    }
  },
  "ui": {
    "pages": [
      {
        "route": "/spec-engine",
        "label": "Spec Engine",
        "icon": "Cog"
      }
    ]
  },
  "backend": {
    "routes": "backend.routes:register_routes",
    "hooks": {
      "on_startup": "startup:on_startup"
    }
  }
}
